#login
  - if @current_user
    %p.welcome  Welcome, #{@current_user.name}!
    = button_to('Log Out', logout_path, :method => :post, class:"uk-button uk-button-text")
  - else
    %p.login= link_to 'Log in with your Facebook account', '/auth/facebook'